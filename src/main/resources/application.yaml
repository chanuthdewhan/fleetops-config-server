spring:
  application:
    name: Config-Server

  cloud:
    config:
      server:
        git:
          uri: git@github.com:chanuthdewhan/fleetops-config-repo.git
          search-paths: platform,services
        native:
          search-locations:
            - classpath:/configurations
            - classpath:/configurations/platform
            - classpath:/configurations/services

server:
  port: 9000


