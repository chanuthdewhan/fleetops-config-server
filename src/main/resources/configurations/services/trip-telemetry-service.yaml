spring:
  mvc:
    problemdetails:
      enabled: true

  mongodb:
    host: localhost
    port: 27080
    database: fleetops_trips
    username: root
    password: mongodb
    authentication-database: admin

server:
  port: 8001